#!/usr/bin/env bash
# Scaffold. Intended to install system deps, enable gpio-ir-tx, create the
# venv, and install a systemd unit on a Raspberry Pi 4.
set -euo pipefail
echo "not implemented" >&2
exit 1
